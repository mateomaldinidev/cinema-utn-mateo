<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<AnchorPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.grupo2.cinemautn.controllers.RegisterController"
            prefWidth="600" prefHeight="520">

    <BorderPane AnchorPane.topAnchor="0.0" AnchorPane.leftAnchor="0.0"
                AnchorPane.rightAnchor="0.0" AnchorPane.bottomAnchor="0.0">

        <!-- HEADER -->
        <top>
            <ToolBar style="-fx-padding:6 12; -fx-background-color:linear-gradient(#ffffff, #e6e6e6);">
                <Button fx:id="btnBack" text="← Volver" onAction="#onBack" />
                <Separator orientation="VERTICAL" />
                <Label text="📝 Registro de Usuario" style="-fx-font-size:16px; -fx-font-weight:bold;" />
                <Pane HBox.hgrow="ALWAYS" />
            </ToolBar>
        </top>

        <!-- FORMULARIO -->
        <center>
            <VBox alignment="TOP_CENTER" spacing="18" style="-fx-padding:24;">
                <Label text="Crea tu cuenta" style="-fx-font-size:14px;" />

                <GridPane hgap="12" vgap="12" alignment="CENTER">
                    <columnConstraints>
                        <ColumnConstraints halignment="RIGHT" minWidth="140" />
                        <ColumnConstraints halignment="LEFT" minWidth="300" />
                    </columnConstraints>

                    <Label text="Nombre completo:" GridPane.rowIndex="0" GridPane.columnIndex="0" />
                    <TextField fx:id="txtNombre" promptText="Nombre y apellido" GridPane.rowIndex="0" GridPane.columnIndex="1" />

                    <Label text="Correo electrónico:" GridPane.rowIndex="1" GridPane.columnIndex="0" />
                    <TextField fx:id="txtCorreo" promptText="correo@ejemplo.com" GridPane.rowIndex="1" GridPane.columnIndex="1" />

                    <Label text="Contraseña:" GridPane.rowIndex="2" GridPane.columnIndex="0" />
                    <PasswordField fx:id="passwordField" promptText="********" GridPane.rowIndex="2" GridPane.columnIndex="1" />

                    <Label text="Repetir contraseña:" GridPane.rowIndex="3" GridPane.columnIndex="0" />
                    <PasswordField fx:id="confirmPasswordField" promptText="********" GridPane.rowIndex="3" GridPane.columnIndex="1" />

                    <Label text="Teléfono:" GridPane.rowIndex="4" GridPane.columnIndex="0" />
                    <TextField fx:id="txtTelefono" promptText="(011) 15-1234-5678" GridPane.rowIndex="4" GridPane.columnIndex="1" />

                    <Label text="Dirección:" GridPane.rowIndex="5" GridPane.columnIndex="0" />
                    <TextField fx:id="txtDireccion" promptText="Calle, número, ciudad" GridPane.rowIndex="5" GridPane.columnIndex="1" />
                </GridPane>

                <HBox alignment="CENTER_LEFT" spacing="8">
                    <CheckBox fx:id="chkTerms" text="Acepto los términos y condiciones" />
                </HBox>

                <HBox alignment="CENTER" spacing="12" style="-fx-padding:8;">
                    <Button fx:id="btnCrear" text="Crear cuenta" onAction="#onCrearUsuario" minWidth="120" />
                    <Button fx:id="btnCancelar" text="Cancelar" onAction="#onCancelar" />
                </HBox>

                <Label fx:id="statusLabel" text="" style="-fx-text-fill:#d00;" />
            </VBox>
        </center>

        <!-- FOOTER -->
        <bottom>
            <HBox style="-fx-padding:8 12; -fx-background-color:#f4f4f4;" alignment="CENTER_LEFT" spacing="10">
                <Label text="Estado:" style="-fx-font-weight:bold;" />
                <Label fx:id="footerStatus" text="Listo" />
            </HBox>
        </bottom>

    </BorderPane>
</AnchorPane>
